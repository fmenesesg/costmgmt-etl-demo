# HCC service account — leave empty in git; set values only in ignored .env at runtime.
COSTMGMT_CLIENT_ID=
COSTMGMT_CLIENT_SECRET=

# Lab Oracle (gvenzl/oracle-free). Placeholder only — never a production password.
ORACLE_DSN=localhost:1521/FREEPDB1
ORACLE_USER=costmgmt
ORACLE_PASSWORD=OracleDemo1
APP_USER=costmgmt
APP_USER_PASSWORD=OracleDemo1

# Lab Apache Superset (compose profile bi). Placeholder only — never a production secret.
SUPERSET_SECRET_KEY=lab-only-not-for-production-change-this-placeholder
